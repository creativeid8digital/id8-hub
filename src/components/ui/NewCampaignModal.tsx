// Replaced by NewJobModal
import type { ViewName } from '@/app/dashboard/page'
type Props = { open: boolean; onClose: () => void; view: ViewName }
export default function NewCampaignModal({ onClose }: Props) { return null }
